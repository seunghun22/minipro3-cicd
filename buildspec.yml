version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  
  build:
    commands:
      - echo "Copying files..."
      - ls -la
      - ls -la src/
      
artifacts:
  files:
    - '**/*'
  base-directory: src
  discard-paths: false